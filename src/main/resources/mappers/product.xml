<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.farmstory.mapper.ProductMapper">

    <insert id="insertProduct">
        insert into product set
        uid=#{uid},
        pname=#{pname},
        cate=#{cate},
        rdate=NOW(),
        price=#{price},
        stock=#{stock},
        delprice=#{delprice},
        company=#{company},
        discount=#{discount},
        point=#{point},
        etc=#{etc}
    </insert>

    <select id="selectProducts" resultType="kr.co.farmstory.dto.ProductDTO">
        SELECT a.* , b.img1 FROM `product` AS a join `img` AS b on a.pno = b.pno
        <where>
            <if test="cate == 'vegetable'"> cate=#{value} </if>
            <if test="cate == 'nuts'"> cate=#{value} </if>
            <if test="cate == 'grains'"> cate=#{value} </if>
            <if test="cate == 'fruit'"> cate=#{value} </if>
            and b.img1 is not NULL
        </where>
    </select>

</mapper>

